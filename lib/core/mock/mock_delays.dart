abstract final class MockDelays {
  static const Duration short = Duration(milliseconds: 800);
  static const Duration medium = Duration(milliseconds: 1500);
  static const Duration long = Duration(milliseconds: 2500);
}
